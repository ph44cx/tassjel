<%= form_with(model: showcase, local: true) do |form| %>
  <% if showcase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(showcase.errors.count, "error") %> prohibited this showcase from being saved:</h2>

      <ul>
      <% showcase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :showcase_title %>
  </div>

  <div class="field">
    <%= form.label :story_image %>
    <%= form.text_field :story_image, id: :showcase_story_image %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle, id: :showcase_subtitle %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :showcase_body %>
  </div>

  <div class="field">
    <%= form.label :share %>
    <%= form.text_field :share, id: :showcase_share %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
